<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Ex2.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Ex2"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Ex2">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Orientation="Horizontal" Margin="10" Grid.ColumnSpan="2" Background="LightGray">
            <TextBox x:Name="tbxTitre" Header="Titre du film" Margin="20" Width="300"/>
            <TextBox x:Name="tbxUrl" Header="URL de l'image" Margin="20" Width="600"/>
            
            <Button x:Name="btnAjout" Content="Ajouter le film" Margin="10" Click="btnAjout_Click"/>
        </StackPanel>

        <ListView x:Name="lvFilms" Height="500" Grid.Row="1" SelectionChanged="lvFilms_SelectionChanged"/>

        <StackPanel Orientation="Vertical" Grid.Row="1" Grid.Column="1" Margin="10" Background="AliceBlue">
            <Image x:Name="imgFilm" Height="200" Margin="10" HorizontalAlignment="Left" VerticalAlignment="Top"/>
            <TextBlock x:Name="tblInfoTitre" FontSize="20" TextAlignment="Start"/>
        </StackPanel>
    </Grid>
</Window>
