<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Ex1.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Ex1"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:Classes="using:Ex1.Classes"
    mc:Ignorable="d"
    Title="Ex1">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal">
            <Button x:Name="btnAjouter" Content="Ajouter les données" Click="btnAjouter_Click"/>
            <ComboBox x:Name="cmbRecherche" SelectedIndex="0" SelectionChanged="cmbRecherche_SelectionChanged" Header="Recherche par catégorie">
                <x:String>Tout</x:String>
                <x:String>condo</x:String>
                <x:String>unifamiliale</x:String>
                <x:String>jumelé</x:String>
            </ComboBox>
            <TextBox x:Name="tbxRecherche" Header="Recherche par ville" TextChanged="tbxRecherche_TextChanged"/>
        </StackPanel>

        <StackPanel Grid.Row="1">
            <ComboBox x:Name="cmbCat" SelectedIndex="0" Header="Catégorie">
                <x:String>condo</x:String>
                <x:String>unifamiliale</x:String>
                <x:String>jumelé</x:String>
            </ComboBox>
            <NumberBox x:Name="nbxPrix" Minimum="0" Header="Prix"/>
            <TextBox x:Name="tbxVille" Header="Ville"/>
        </StackPanel>

        <ListView x:Name="lvListeMaisons" Grid.Row="2">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="Classes:Maison">
                    <StackPanel Orientation="Vertical" Spacing="10" Background="AliceBlue" Margin="5" Padding="5">
                        <TextBlock Text="{x:Bind Categorie, Mode=OneWay}" FontSize="20"/>
                        <TextBlock Text="{x:Bind Prix, Mode=OneWay}" FontSize="20"/>
                        <TextBlock Text="{x:Bind Ville, Mode=OneWay}" FontSize="20"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Window>
