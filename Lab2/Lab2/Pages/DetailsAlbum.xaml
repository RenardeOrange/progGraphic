<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Lab2.Pages.DetailsAlbum"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Lab2.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Image x:Name="AlbumCover" Width="200" Height="200" Margin="10"/>

        <StackPanel Grid.Column="1" Margin="10">
            <TextBox x:Name="TbxNom" Header="Nom de l'album"/>
            <TextBlock x:Name="TbkNom" Foreground="Red"/>
            
            <TextBox x:Name="TbxAuteur" Header="Nom de l'artiste / groupe"/>
            <TextBlock x:Name="TbkAuteur" Foreground="Red"/>
            
            <TextBox x:Name="TbxLabel" Header="Nom du label"/>
            <TextBlock x:Name="TbkLabel" Foreground="Red"/>

            <TextBox x:Name="TbxCouverture" Header="Couverture"/>
            <TextBlock x:Name="TbkCouverture" Foreground="Red"/>

            <Button x:Name="BtnValider" Click="BtnValider_Click" Background="LightGreen" Content="Créer"/>
        </StackPanel>
    </Grid>
</Page>
