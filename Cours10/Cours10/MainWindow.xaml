<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Cours10.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Cours10"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Cours10">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <StackPanel>
        <ListView x:Name="lvClients">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:Client">
                    <StackPanel Orientation="Vertical" Spacing="10" Background="AliceBlue" Margin="5" Padding="5">
                        <TextBlock Text="{x:Bind Nom, Mode=OneWay}" FontSize="20"/>
                        <TextBox Text="{x:Bind Age, Mode=TwoWay}" FontSize="20"/>
                        <Button Click="Button_Click" HorizontalAlignment="Center">
                            <SymbolIcon Symbol="Delete" />
                        </Button>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <Button x:Name="btnDemo" Click="btnDemo_Click" Content="Sort"/>
    </StackPanel>
</Window>
