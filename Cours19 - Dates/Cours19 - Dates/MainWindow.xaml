<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Cours19___Dates.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Cours19___Dates"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Cours19 - Dates">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel>
            <Button x:Name="BtnAfficherDate" Content="Afficher la date actuelle" Click="BtnAfficherDate_Click" Margin="10"/>
            <DatePicker x:Name="DatePicker1" Margin="10" DateChanged="DatePicker1_DateChanged"/>
            <CalendarDatePicker x:Name="CalendarDatePicker1" Margin="10"/>
            <CalendarView x:Name="calendarView"
                        SelectedDatesChanged="calendarView_SelectedDatesChanged"
                        SelectionMode="Multiple">
            </CalendarView>
            <TimePicker x:Name="TimePickerDemo" TimeChanged="TimePickerDemo_TimeChanged" ClockIdentifier="24HourClock" MinuteIncrement="30"/>
        </StackPanel>

        <StackPanel Grid.Row="1">
            <TextBlock x:Name="TblDate" FontSize="16" Margin="10"/>
            <TextBlock x:Name="TblDate2" FontSize="16" Margin="10"/>
            <TextBlock x:Name="TblDate3" FontSize="16" Margin="10"/>
        </StackPanel>

    </Grid>
</Window>
